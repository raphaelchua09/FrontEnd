export interface Filter {
  filterValue: boolean;
  status: boolean;
    statusValue: string;
  gender: boolean;  
    male: boolean;
    female: boolean;
    others: boolean;
  birthdate: boolean;  
    startDate: string;
    endDate: string;
  }